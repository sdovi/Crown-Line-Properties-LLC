# 🚀 Быстрый деплой на Vercel

## Проблема: Стили не подгружаются

**Если вы открываете `index.html` локально и стили не работают:**

### ❌ Неправильно:
```bash
npm run build
# Открываете out/index.html - стили НЕ работают
```

### ✅ Правильно для локального просмотра:
```bash
npm run build:static
# Открываете out/index.html - стили РАБОТАЮТ
```

---

## 🎯 Деплой на Vercel (САМЫЙ ПРОСТОЙ СПОСОБ)

### Способ 1: Через GitHub (РЕКОМЕНДУЕТСЯ)

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
   git push -u origin main
   ```

2. **Зайдите на [vercel.com](https://vercel.com)**
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий
   - Vercel автоматически определит Next.js
   - Нажмите "Deploy"

3. **Готово!** Ваш сайт будет доступен по адресу `ваш-проект.vercel.app`

**Vercel автоматически:**
- Определит Next.js
- Использует `npm run build`
- Задеплоит проект
- Настроит все автоматически

---

### Способ 2: Через Vercel CLI

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Войдите в Vercel
vercel login

# 3. Задеплойте (в корне проекта)
vercel

# 4. Следуйте инструкциям
# - Выберите проект или создайте новый
# - Нажмите Enter для всех настроек по умолчанию

# Для продакшена:
vercel --prod
```

---

## ⚙️ Важно: Настройка для Vercel

**Для Vercel НЕ нужно менять конфигурацию!**

Vercel автоматически:
- Определит, что это Next.js проект
- Использует правильную команду сборки
- Настроит все автоматически

**Текущая конфигурация (`output: 'export'`) будет работать на Vercel**, но Vercel может предложить использовать обычный Next.js режим для лучшей производительности.

---

## 🔧 Если нужны переменные окружения (Telegram)

1. Зайдите в настройки проекта на Vercel
2. Settings → Environment Variables
3. Добавьте:
   - `NEXT_PUBLIC_TELEGRAM_BOT_TOKEN` = ваш токен
   - `NEXT_PUBLIC_TELEGRAM_CHAT_ID` = ваш chat ID
4. Пересоберите проект (Redeploy)

---

## 📝 Резюме

**Для деплоя на Vercel:**
- ✅ Используйте GitHub или Vercel CLI
- ✅ Не нужно менять конфигурацию
- ✅ Vercel все сделает автоматически

**Для локального просмотра HTML:**
- ✅ Используйте `npm run build:static`
- ✅ Откройте `out/index.html`

**Проблема со стилями:**
- ❌ `npm run build` → стили не работают локально
- ✅ `npm run build:static` → стили работают локально
- ✅ На Vercel стили работают в обоих случаях

---

## 🎯 Самый быстрый способ

```bash
# 1. Загрузите на GitHub
git init
git add .
git commit -m "Deploy to Vercel"
git remote add origin https://github.com/ваш-username/репозиторий.git
git push -u origin main

# 2. Зайдите на vercel.com
# 3. Add New Project → выберите репозиторий → Deploy
# 4. Готово! 🎉
```
