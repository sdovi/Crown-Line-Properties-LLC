# 📦 Инструкция по деплою статического HTML сайта

## ✅ Готово! Сайт собран и готов к деплою

### 🚀 Быстрый старт

**Для создания статического HTML сайта используйте:**

```bash
npm run build:static
```

Эта команда:
1. Соберет проект (`next build`)
2. Автоматически исправит пути в HTML файлах для работы локально

### 📁 Где находятся файлы

Все готовые файлы находятся в папке **`out/`**:

```
out/
├── index.html          ← Главная страница (откройте в браузере!)
├── logo.jpg           ← Логотип
├── 404.html           ← Страница ошибки
└── _next/             ← JavaScript и CSS файлы
    └── static/
        ├── css/       ← Стили (все подключены!)
        └── chunks/    ← JavaScript код
```

### 🖥️ Как открыть локально

**Вариант 1: Прямо в браузере**
- Просто откройте файл `out/index.html` в браузере
- Все стили и скрипты будут работать!

**Вариант 2: Через локальный сервер (рекомендуется)**
```bash
# Python
cd out
python -m http.server 8000

# Node.js (если установлен http-server)
npx http-server out -p 8000

# PHP
cd out
php -S localhost:8000
```

Затем откройте `http://localhost:8000` в браузере.

### 🌐 Как задеплоить на хостинг

**1. GitHub Pages:**
```bash
# Создайте репозиторий на GitHub
# Загрузите содержимое папки out/ в ветку gh-pages
# Или используйте GitHub Actions для автоматического деплоя
```

**2. Netlify:**
- Перетащите папку `out/` на [netlify.com/drop](https://app.netlify.com/drop)
- Или подключите репозиторий GitHub

**3. Vercel:**
```bash
# Установите Vercel CLI
npm i -g vercel

# Задеплойте
cd out
vercel
```

**4. Любой статический хостинг:**
- Загрузите **всё содержимое** папки `out/` на ваш хостинг
- Убедитесь, что `index.html` находится в корне

### ⚙️ Настройка формы (опционально)

Если хотите, чтобы форма отправляла заявки в Telegram:

1. Создайте файл `.env.local` в корне проекта:
```env
NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=ваш_токен_бота
NEXT_PUBLIC_TELEGRAM_CHAT_ID=ваш_chat_id
```

2. Пересоберите проект:
```bash
npm run build:static
```

**Важно:** Для статического экспорта переменные должны начинаться с `NEXT_PUBLIC_`, чтобы они были доступны в браузере.

### 📝 Примечания

- Все пути в HTML файлах автоматически исправлены на относительные
- Сайт работает без сервера (можно открыть `index.html` напрямую)
- Все стили и скрипты подключены корректно
- Логотип находится в `out/logo.jpg`

### 🔄 Обновление сайта

После любых изменений в коде:
```bash
npm run build:static
```

Новые файлы будут в папке `out/` - просто загрузите их на хостинг.

---

**Готово! Ваш статический HTML сайт готов к использованию.** 🎉
