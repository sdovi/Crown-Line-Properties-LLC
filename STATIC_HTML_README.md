# ✅ Статический HTML сайт готов!

## 📁 Где находятся файлы

Все статические HTML файлы находятся в папке **`out/`**

## 🚀 Как использовать

### Вариант 1: Открыть локально
Просто откройте файл `out/index.html` в браузере

### Вариант 2: Загрузить на хостинг
Загрузите **всё содержимое** папки `out/` на любой статический хостинг:
- GitHub Pages
- Netlify
- Vercel
- Любой веб-сервер

## 📂 Структура файлов

```
out/
├── index.html          ← Главная страница (откройте этот файл)
├── logo.jpg           ← Логотип
├── 404.html           ← Страница ошибки 404
└── _next/             ← JavaScript и CSS файлы
    └── static/
        ├── css/       ← Стили
        └── chunks/    ← JavaScript код
```

## ⚙️ Настройка формы (опционально)

Если хотите, чтобы форма отправляла заявки в Telegram:

1. Создайте файл `.env.local` в корне проекта:
```env
NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=ваш_токен
NEXT_PUBLIC_TELEGRAM_CHAT_ID=ваш_chat_id
```

2. Пересоберите проект:
```bash
npm run build
```

**Важно:** Токен будет виден в коде клиента, поэтому используйте только публичные боты.

## 🔄 Пересборка

Если вы внесли изменения в код, пересоберите проект:

```bash
npm run build
```

Новые файлы появятся в папке `out/`

## 📝 Примечания

- Все анимации и интерактивность работают в статическом HTML
- Форма будет работать только если настроены переменные окружения
- Изображения оптимизированы для веба
- Сайт полностью готов к развертыванию

---

**Готово!** Откройте `out/index.html` в браузере или загрузите папку `out/` на хостинг.
