# 🚀 Инструкция по деплою на Vercel

## ⚠️ Важно: Два варианта деплоя

### Вариант 1: Деплой как Next.js приложение (РЕКОМЕНДУЕТСЯ для Vercel)

Vercel отлично работает с Next.js. Это лучший вариант для вашего проекта.

#### Шаг 1: Уберите статический экспорт

Временно измените `next.config.js`:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // УБРАТЬ: output: 'export',
  images: {
    unoptimized: true,
    domains: ['images.unsplash.com', 'via.placeholder.com'],
    formats: ['image/avif', 'image/webp'],
  },
  trailingSlash: true,
}

module.exports = nextConfig
```

#### Шаг 2: Деплой через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Задеплойте проект
vercel

# Для продакшена
vercel --prod
```

#### Шаг 3: Или через GitHub

1. Создайте репозиторий на GitHub
2. Загрузите код в репозиторий
3. Зайдите на [vercel.com](https://vercel.com)
4. Нажмите "Add New Project"
5. Выберите ваш репозиторий
6. Vercel автоматически определит Next.js и задеплоит

**Vercel автоматически:**
- Определит Next.js
- Настроит сборку (`npm run build`)
- Задеплоит проект
- Настроит переменные окружения (если нужно)

---

### Вариант 2: Статический HTML (если нужен именно статический сайт)

Если вам нужен именно статический HTML сайт:

#### Шаг 1: Соберите статический сайт

```bash
npm run build:static
```

#### Шаг 2: Деплой папки `out/`

**Через Vercel CLI:**
```bash
# Установите Vercel CLI
npm i -g vercel

# Перейдите в папку out
cd out

# Задеплойте
vercel

# Для продакшена
vercel --prod
```

**Через веб-интерфейс Vercel:**
1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New Project"
3. Выберите "Upload" (вместо GitHub)
4. Загрузите папку `out/` (можно заархивировать в zip)

---

## 🔧 Настройка переменных окружения (для Telegram)

Если используете форму с отправкой в Telegram:

1. Зайдите в настройки проекта на Vercel
2. Перейдите в "Environment Variables"
3. Добавьте:
   - `NEXT_PUBLIC_TELEGRAM_BOT_TOKEN` = ваш токен
   - `NEXT_PUBLIC_TELEGRAM_CHAT_ID` = ваш chat ID

**Важно:** После добавления переменных нужно пересобрать проект.

---

## ❌ Проблема: Стили не подгружаются при открытии index.html

Если вы открываете `index.html` локально и стили не работают:

### Причина:
- Вы используете `npm run build` вместо `npm run build:static`
- Пути в HTML не исправлены на относительные

### Решение:

**Для локального просмотра:**
```bash
# Используйте правильную команду
npm run build:static

# Затем откройте out/index.html
```

**Для деплоя на Vercel:**
- Используйте **Вариант 1** (обычный Next.js) - это лучший вариант
- Или используйте **Вариант 2** и загрузите папку `out/`

---

## 📝 Рекомендация

**Для Vercel используйте Вариант 1** (обычный Next.js):
- ✅ Автоматическая оптимизация
- ✅ SSR/SSG поддержка
- ✅ Автоматические обновления при push в GitHub
- ✅ Лучшая производительность
- ✅ Проще настройка

Статический экспорт (`output: 'export'`) нужен только если:
- Вы хотите загрузить сайт на обычный хостинг без Node.js
- Нужен полностью статический HTML

---

## 🎯 Быстрый старт (рекомендуемый способ)

```bash
# 1. Уберите output: 'export' из next.config.js (временно)

# 2. Установите Vercel CLI
npm i -g vercel

# 3. Задеплойте
vercel

# 4. Следуйте инструкциям в терминале
```

Готово! 🎉
